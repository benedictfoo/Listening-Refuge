---
const {date, updated} = Astro.props;
---

<time class="text-lg font-semibold" datetime={date.toISOString()}>
    {updated ? 'Updated on ' : 'Created on '}{
    date.toLocaleDateString('en-us', {
        year: 'numeric',
        month: 'short',
        day: 'numeric',
    })
}
</time>
